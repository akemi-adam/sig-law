#ifndef VALIDATION
#define VALIDATION

#include <stdbool.h>

bool isString(const char*);

bool isEmpty(const char*);

bool isPositive(int);

bool isEmail(char[55]);

bool isTelephone(const char[14]);

bool isCpf(char[12]);



#endif